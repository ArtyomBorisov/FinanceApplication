spring:
  mail:
    host : smtp.mail.ru
    port : 465
    username : "${email.login}"
    password : "${email.password}"
    protocol : smtps
    from : "${email.login}"
    auth : true

report_service_url : http://report-service:8080/report/%s/export

#server:
#  port : 8085
#
#report_service_url : http://localhost:8084/report/%s/export